# CLAUDE.md

## 프로젝트 개요

이 저장소는 캡스톤 프로젝트 **Scuppy**를 위한 개발 저장소입니다.

주요 목표는 다음과 같습니다.
- IR 카메라 기반 운전자 모니터링 시스템(DMS) 구현
- 저조도 환경에서 얼굴, 시선, 자세 추정 성능 확보
- 졸음, 시선 이탈, 주의 분산 같은 이상 상태 감지
- 실시간 추론이 가능한 형태로 모델과 파이프라인 정리

노션 기준 현재 프로젝트 방향은 다음을 포함합니다.
- RGB 기반 기존 방식의 한계를 보완하기 위한 IR 카메라 활용
- 얼굴 정보와 상반신/전신 포즈를 결합하는 멀티태스크 융합 모델
- 얼굴 confidence를 활용한 동적 가중 또는 보정 전략
- 자체 촬영 데이터셋 구축과 augmentation 기반 백업 플랜 병행

이 문서는 Claude 계열 코딩 어시스턴트가 이 저장소에서 작업할 때 따라야 하는 실무 가이드입니다.

## Git 브랜치 전략

- `main`: 공유 가능한 안정 버전
- `feature/face`: 얼굴, 눈, 랜드마크, 시선 추정 관련 작업
- `feature/pose`: 자세 추정, 상반신/머리 포즈 관련 작업
- `feature/fusion`: 얼굴 + 포즈 결합 로직, 상태 판별 로직
- `feature/deploy`: TensorRT 최적화, 임베디드 배포, 실시간 추론 파이프라인
- `docs/*`: 문서 작업

브랜치는 작업 목적이 드러나도록 생성합니다.

예시:

```bash
git checkout -b feature/face
git checkout -b feature/fusion
git checkout -b docs/readme-update
```

현재 노션에 정리된 팀 역할 배분은 아래와 같습니다.

- 얼굴: 강성찬, 유건
- 포즈: 이수찬, 황영인
- 조율 및 통합 지원: 강민성

Claude는 가능하면 작업 범위가 이 역할 구분과 충돌하지 않도록 문맥을 확인하고 수정합니다.
특히 조율 및 통합 지원 파트는 전체 시스템 흐름, 베이스라인 정리, 임베디드 배포 연결 작업과 맞닿아 있으므로 관련 변경 시 문서와 구조를 함께 살핍니다.

## 커밋 컨벤션

```text
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
refactor: 코드 구조 개선
test: 테스트 추가 또는 수정
chore: 기타 설정 및 정리
```

예시:

```bash
git commit -m "feat: gaze estimation baseline 추가"
git commit -m "fix: IR frame preprocessing 오류 수정"
git commit -m "docs: 모델 실행 절차 정리"
```

## 권장 작업 흐름

1. 작업용 브랜치 생성
2. 관련 모듈 구현 또는 수정
3. 가능한 범위에서 실행 확인 또는 실험 검증
4. 변경 내용 문서화
5. 커밋 후 PR 준비

Claude는 한 번에 너무 많은 범위를 건드리지 말고, 리뷰 가능한 단위로 작업합니다.

## 개발 환경 원칙

구체적인 실행 명령은 실제 저장소 구조에 맞춰 조정하되, 아래 원칙을 따릅니다.

- Python 기반 ML/비전 프로젝트를 기본 전제로 합니다.
- 환경 의존성이 있으면 `requirements.txt`, `pyproject.toml`, `environment.yml` 같은 파일로 관리합니다.
- CUDA, TensorRT, OpenCV, PyTorch 같은 런타임 의존성은 버전 충돌 가능성을 고려합니다.
- 절대경로를 코드에 직접 넣지 않습니다.
- 데스크톱 데모만이 아니라 임베디드 환경 이식 가능성도 함께 고려합니다.

환경 설정 예시는 추후 저장소 구조가 정리되면 실제 명령으로 업데이트합니다.

## 모듈 구현 가이드

새로운 모델이나 파이프라인을 추가할 때는 아래를 명확히 해야 합니다.

- 입력 형식
- 출력 형식
- 전처리 방식
- 후처리 방식
- 사용하는 모델 weight 또는 config
- 실패 시 동작 방식

예를 들어 gaze, pose, fusion 모듈은 각각 아래 질문에 답할 수 있어야 합니다.

- 어떤 입력을 받는가
- 어떤 값이나 벡터를 반환하는가
- confidence는 어떻게 계산하는가
- inference latency에 어떤 영향을 주는가

또한 이 프로젝트에서는 아래 두 축을 명확히 구분해서 다루는 것을 권장합니다.

- `end-to-end model`: 입력에서 최종 이상 상태 분류까지 하나의 모델 흐름으로 처리하는 접근
- `application pipeline`: 전처리, 얼굴/시선, 포즈, fusion, alert 단계를 나누는 시스템 접근

Claude는 새 구현이 어느 범주에 속하는지 가능한 한 명시적으로 드러내야 합니다.

예시:
- 파일 경로에 `end_to_end`, `pipeline`, `fusion` 같은 이름 사용
- 실험 문서에 baseline이 end-to-end인지 score-based pipeline인지 구분
- 커밋 메시지나 문서에서 접근 방식을 명확히 설명

## 실험 코드 작성 규칙

- 베이스라인과 개선안을 비교하기 쉽게 작성합니다.
- 한 번에 하나의 큰 변수만 바꾸는 실험을 우선합니다.
- 학습 코드와 추론 코드는 가능하면 역할을 분리합니다.
- metric 정의는 평가 코드 근처에 남깁니다.
- 실험 결과는 나중에 비교할 수 있게 이름과 로그 형식을 통일합니다.
- 자체 데이터셋 실험과 기존 데이터셋/증강 실험은 기록상 구분합니다.

가능하면 다음 정보를 함께 남깁니다.

- 사용 데이터셋 또는 split
- 입력 해상도
- batch size
- 주요 threshold
- latency 또는 FPS
- accuracy 관련 metric

## 실시간 추론 관련 주의사항

이 프로젝트는 운전자 모니터링 시스템이므로 아래 요소를 특히 주의합니다.

- 불필요한 CPU-GPU 전송
- 과도한 전처리 비용
- hidden threshold에 의존하는 불안정한 fusion 규칙
- 저조도 또는 가림 상황에서의 성능 저하
- missing frame 또는 얼굴 미검출 시의 fallback 처리

특히 score-based pipeline을 사용할 경우, 얼굴 confidence와 포즈 비중 조절 규칙이 코드에 드러나야 하며 실험 없이 감으로 튜닝한 threshold만 남지 않도록 주의합니다.

Claude는 정확도만이 아니라 지연 시간과 실제 사용 가능성도 함께 고려해야 합니다.

## 문서화 원칙

코드 구조, 실험 방식, 평가 기준이 바뀌면 문서도 함께 갱신합니다.

특히 다음이 바뀌면 README나 관련 문서를 함께 수정하는 것을 권장합니다.

- 실행 방법
- 모델 구조
- 데이터 준비 방식
- 평가 방식
- 저장소 폴더 구조
- end-to-end 접근과 pipeline 접근 중 어느 방향인지

## 하지 말아야 할 것

- 거대한 단일 스크립트에 모든 로직 몰아넣기
- 개인 PC 전용 경로 하드코딩
- 설명 없는 magic number 남기기
- 실험 코드와 운영 코드를 경계 없이 섞기
- 요청 없이 사용자 변경사항 덮어쓰기
- 요청 없이 push, force push, reset 같은 파괴적 작업 수행

## Claude 기본 행동 원칙

Claude는 이 저장소에서 작업할 때 다음을 우선합니다.

- 먼저 저장소 구조와 관련 파일을 읽고 이해할 것
- 가장 작은 변경으로 가장 큰 명확성을 만들 것
- 불확실한 부분은 추측보다 명시적으로 드러낼 것
- 재현 가능성, 정확도, latency 관점의 리스크를 함께 볼 것
- push나 병합은 사용자가 직접 검토할 수 있도록 맡길 것

## 추후 채우면 좋은 항목

아래 항목은 팀 논의 후 더 구체적으로 채울 수 있습니다.

- 팀원별 담당 파트
- 실제 디렉터리 구조
- 데이터셋 출처와 관리 방식
- 모델별 baseline metric
- 데모 실행 명령
- PR 규칙과 리뷰 기준

# CLAUDE.md

## 프로젝트 요약

Scuppy는 IR 기반 운전자 모니터링 시스템 프로젝트입니다.

핵심 목표:
- 저조도 환경에서 얼굴, 시선, 자세 추정
- 얼굴 + 포즈 기반 이상 상태 감지
- confidence 기반 융합
- 실시간 추론과 임베디드 배포 가능성 확보

## 역할과 브랜치

- 얼굴: 강성찬, 유건
- 포즈: 이수찬, 황영인
- 조율 및 시스템 통합: 강민성

권장 브랜치:
- `feature/face`
- `feature/pose`
- `feature/fusion`
- `feature/deploy`
- `docs/*`

`feature/deploy`는 TensorRT 최적화, 임베디드 배포, 실시간 추론 파이프라인 작업에 사용합니다.

## Claude 작업 원칙

- 먼저 저장소 구조와 관련 파일을 읽고 이해합니다.
- 한 번에 너무 많은 범위를 건드리지 말고 리뷰 가능한 단위로 수정합니다.
- 절대경로와 개인 환경 의존 설정을 피합니다.
- push, reset, force push 같은 파괴적 작업은 요청 없이는 하지 않습니다.
- 문서, 구조, 평가 기준이 바뀌면 관련 문서도 함께 갱신합니다.
- 임베디드 배포나 실시간 추론으로 이어질 수 있는 변경은 latency와 의존성 영향을 함께 봅니다.

## 구현 원칙

새 모델이나 파이프라인에는 가능하면 아래가 드러나야 합니다.
- 입력 형식
- 출력 형식
- 전처리/후처리 방식
- 사용하는 weight 또는 config
- 실패 시 동작 방식

이 프로젝트에서는 아래 두 접근을 구분합니다.
- `end-to-end model`: 입력에서 최종 상태 판별까지 하나의 모델 흐름으로 처리하는 접근
- `application pipeline`: 입력, 전처리, 얼굴/시선 추정, 포즈 추정, fusion, alert 단계를 연결한 전체 시스템 흐름

Claude는 새 구현이 어느 쪽인지 코드 경로, 문서, 커밋 메시지에서 가능한 한 명확히 드러내야 합니다.
특히 얼굴 confidence를 활용한 fusion 또는 동적 제어 규칙은 코드와 문서에 함께 남기는 것을 권장합니다.
또한 얼굴 파트와 포즈 파트가 분리되어 진행되더라도, 최종적으로는 하나의 시스템으로 통합된다는 점을 기준으로 구조를 판단합니다.

## 실험 원칙

- 베이스라인과 개선안을 비교하기 쉽게 작성합니다.
- 한 번에 하나의 큰 변수만 바꾸는 실험을 우선합니다.
- 학습 코드와 추론 코드는 가능하면 역할을 분리합니다.
- 자체 데이터셋 실험과 기존 데이터셋/증강 실험은 기록상 구분합니다.
- 얼굴 confidence와 포즈 비중 조절 규칙은 코드와 문서에 남깁니다.
- score-based pipeline 실험이라면 threshold와 가중치 기준을 함께 남깁니다.
- 공개 데이터셋은 용량과 라벨 기준을 확인하고 필요한 파트만 선별해 사용하는 방향을 우선합니다.

가능하면 함께 남길 것:
- 사용 데이터셋 또는 split
- 주요 threshold
- latency 또는 FPS
- accuracy 관련 metric

## 커밋 형식

```text
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
refactor: 코드 구조 개선
test: 테스트 추가 또는 수정
chore: 기타 설정 및 정리
```

# AGENTS.md

## 프로젝트 개요

이 저장소는 캡스톤 프로젝트 **Scuppy**의 개발 및 협업을 위한 저장소입니다.

프로젝트의 핵심 목표는 다음과 같습니다.
- IR 카메라 기반 운전자 모니터링 시스템(DMS) 구현
- 저조도 환경에서도 안정적으로 얼굴, 시선, 자세 정보를 추정
- 졸음, 주의 분산, 전방 미주시 등 이상 상태를 탐지
- 실시간 추론이 가능한 구조로 모델과 파이프라인을 정리

노션 문서 기준으로 현재 프로젝트는 다음 기술 방향을 중요하게 다룹니다.
- 얼굴 정보와 상반신/전신 포즈를 결합하는 멀티태스크 융합 접근
- 얼굴 인식 confidence를 활용한 동적 제어 방식
- 자체 데이터셋 구축과 기존 데이터셋 기반 증강 백업 플랜 병행
- 저조도, 급격한 조도 변화, 마스크/선글라스 등 차폐 상황 대응

이 문서는 저장소에서 작업하는 팀원과 코딩 에이전트가 공통으로 따를 기본 원칙을 정의합니다.

## 기본 원칙

- 실험용 코드와 재사용 가능한 코드는 분리합니다.
- 한 번만 실행하는 임시 코드보다 다시 실행 가능한 구조를 우선합니다.
- 절대경로, 개인 PC 전용 설정, 임시 파일 경로를 코드에 직접 넣지 않습니다.
- 큰 기능은 한 번에 몰아서 추가하지 말고, 작은 단위로 나누어 변경합니다.
- 성능 향상을 주장할 때는 가능하면 근거가 되는 실험 결과나 로그를 함께 남깁니다.

## 권장 디렉터리 방향

저장소가 커질수록 아래와 같이 책임을 분리하는 방향을 권장합니다.

- `data/`: 데이터 전처리, split, 메타데이터 관리
- `models/`: 얼굴, 시선, 포즈, fusion 모델 정의
- `training/`: 학습 스크립트, 학습 설정, 평가 루프
- `inference/`: 실시간 추론 파이프라인
- `experiments/`: 베이스라인 비교, 실험 기록용 코드
- `docs/`: 설계 문서, 회의 정리, 실험 요약
- `assets/`: 문서, 데모, 임베디드 배포 준비에 필요한 정적 리소스

현재 구조가 달라도 새 코드는 비슷한 책임끼리 모이도록 유지합니다.

## Git 작업 규칙

- `main`은 항상 공유 가능한 상태를 유지합니다.
- 기능 개발은 별도 브랜치에서 진행합니다.
- 브랜치 이름은 작업 목적이 드러나도록 작성합니다.

예시:
- `feature/face`
- `feature/pose`
- `feature/fusion`
- `feature/deploy`
- `docs/readme-update`

현재 노션에 정리된 팀 역할 배분은 아래와 같습니다.
- 얼굴: 강성찬, 유건
- 포즈: 이수찬, 황영인
- 조율 및 통합 지원: 강민성

조율 및 통합 지원 역할은 팀 간 진행 상황 연결, 전체 시스템 베이스라인 정리, 통합 이슈 조율, 임베디드 배포 관점의 연결 작업을 포함합니다.

팀 역할이 바뀌면 이 문서와 관련 README를 함께 갱신합니다.

## 커밋 규칙

아래 형식을 기본으로 사용합니다.

```text
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
refactor: 구조 개선
test: 테스트 추가 또는 수정
chore: 기타 설정, 환경 정리
```

예시:

```bash
git commit -m "feat: IR 얼굴 랜드마크 전처리 추가"
git commit -m "fix: gaze vector shape mismatch 수정"
git commit -m "docs: 실험 실행 방법 문서화"
```

## 협업 시 주의사항

- 원본 데이터셋, 대용량 weight 파일, 실험 산출물 전체를 무분별하게 커밋하지 않습니다.
- 노트북에서 검증한 로직은 가능하면 `.py` 모듈로 옮깁니다.
- 모델 입력 형식과 출력 형식은 주석 또는 문서에 남깁니다.
- 전처리 규칙이 학습과 추론에서 다르면 반드시 이유를 문서화합니다.
- confidence threshold, smoothing 값, fusion 규칙 같은 핵심 숫자는 의미 없이 하드코딩하지 않습니다.
- 자체 촬영 데이터셋 구축이 지연될 경우를 대비해 augmentation 기반 백업 플랜을 항상 유지합니다.

## 모델과 파이프라인 구분

이 프로젝트에서는 아래 두 가지를 명확히 구분해 관리하는 것을 권장합니다.

- `end-to-end model`: 입력에서 최종 상태 판별까지 하나의 모델 또는 단일 학습 흐름으로 처리하는 접근
- `application pipeline`: 전처리, 얼굴 검출, 시선 추정, 포즈 추정, fusion, alert 단계를 나누어 구성하는 접근

새 코드나 실험을 추가할 때는 가능한 경우 둘 중 어느 범주인지 문서, 브랜치 이름, 실험 기록에 명확히 드러내는 것을 권장합니다.

예시:
- `experiments/end_to_end/`
- `experiments/pipeline/`
- `feature/fusion-score`
- `feature/end2end-baseline`

## 실험 관리 원칙

의미 있는 실험은 아래 질문에 답할 수 있어야 합니다.

- 무엇을 바꿨는가?
- 왜 바꿨는가?
- 어떤 데이터 또는 split을 사용했는가?
- 어떤 metric이 좋아졌거나 나빠졌는가?
- 다른 팀원이 다시 실행할 수 있는가?

가능하면 실험 이름, 로그, 결과 파일 이름을 일관되게 관리합니다.

## 품질 기준

큰 변경을 반영하기 전에는 가능하면 아래를 확인합니다.

- 다른 환경에서도 실행 가능한가
- 빠진 weight, config, input이 있을 때 에러가 명확한가
- 추론 파이프라인의 입출력이 문서와 일치하는가
- 성능 측정 기준이 코드에 반영되어 있는가
- 문서가 실제 코드 상태를 따라가고 있는가
- 얼굴, 포즈, fusion 각 단계의 역할이 end-to-end 접근과 혼동되지 않는가

## 에이전트 작업 규칙

자동화 코딩 에이전트는 다음을 지켜야 합니다.

- 수정 전 관련 파일과 문맥을 먼저 읽습니다.
- 사용자가 만든 변경 사항을 덮어쓰지 않습니다.
- 요청받지 않은 destructive git 작업은 하지 않습니다.
- 모르는 프로젝트 사실을 임의로 지어내지 않습니다.
- 가능한 한 작고 검토 가능한 단위로 수정합니다.
- 실험 코드와 배포용 코드를 섞을 때는 위험 요소를 먼저 설명합니다.

## 우선순위

이 저장소에서는 아래 우선순위를 권장합니다.

1. 재현 가능성
2. 정확도
3. 실시간성
4. 구조적 확장성
5. 문서화

즉, "한 번 우연히 잘 되는 코드"보다 "다시 돌릴 수 있고 비교할 수 있는 코드"를 더 높게 평가합니다.

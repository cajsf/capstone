# AGENTS.md

## 프로젝트

Scuppy는 IR 카메라 기반 운전자 모니터링 시스템(DMS) 캡스톤 프로젝트입니다.

핵심 방향:
- 저조도와 차폐 상황에 강인한 인식
- 얼굴/시선 + 포즈를 결합한 멀티태스크 융합
- 얼굴 confidence 기반 동적 제어
- 자체 데이터셋 구축과 augmentation 백업 플랜 병행
- 실시간 추론 및 임베디드 배포 고려

## 역할

- 얼굴: 강성찬, 유건
- 포즈: 이수찬, 황영인
- 조율 및 시스템 통합: 강민성

조율 및 시스템 통합은 팀 간 진행 조율, 베이스라인 정리, 통합 구조 설계, 임베디드 배포 연결 작업을 포함합니다.

## 작업 원칙

- 임시 코드보다 재현 가능한 구조를 우선합니다.
- 절대경로, 개인 환경 의존 설정, 대용량 산출물 커밋을 피합니다.
- 노트북에서 검증한 로직은 가능하면 모듈로 옮깁니다.
- 모델 입력/출력, 핵심 threshold, fusion 규칙은 문서나 코드에 남깁니다.
- 큰 기능은 작은 단위로 나눠서 수정합니다.
- 자체 데이터셋과 augmentation 기반 실험은 기록상 구분하는 것을 권장합니다.
- 공개 데이터셋은 용량, 라벨 정의, 활용 목적을 확인한 뒤 필요한 파트만 선별해 사용하는 것을 권장합니다.

## 구조 원칙

권장 책임 분리:
- `data/`
- `models/`
- `training/`
- `inference/`
- `experiments/`
- `docs/`

또한 아래 두 방향을 구분해 관리합니다.
- `end-to-end model`: 입력에서 최종 상태 판별까지 하나의 모델 흐름으로 처리하는 접근
- `application pipeline`: 입력, 전처리, 얼굴/시선 추정, 포즈 추정, fusion, alert 단계를 연결한 전체 시스템 흐름

새 코드와 실험은 가능한 경우 어느 범주인지 드러나게 작성합니다.
또한 얼굴 파트와 포즈 파트는 분리해 개발하되, 최종적으로는 하나의 DMS 시스템으로 통합된다는 전제를 유지합니다.

## Git 규칙

- `main`은 공유 가능한 상태를 유지합니다.
- 기능 개발은 별도 브랜치에서 진행합니다.
- 권장 브랜치: `feature/face`, `feature/pose`, `feature/fusion`, `feature/deploy`, `docs/*`

커밋 형식:

```text
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
refactor: 구조 개선
test: 테스트 추가 또는 수정
chore: 기타 설정 및 정리
```

## 에이전트 규칙

- 수정 전 관련 파일을 먼저 읽습니다.
- 사용자 변경 사항을 덮어쓰지 않습니다.
- 요청 없는 destructive git 작업은 하지 않습니다.
- 실험 코드와 배포 코드를 섞을 때는 위험을 먼저 설명합니다.
- 문서와 실제 코드 상태가 어긋나면 함께 갱신합니다.
- 임베디드 배포나 실시간 추론과 연결되는 변경은 latency와 의존성도 함께 고려합니다.

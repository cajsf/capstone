#운전자 모니터링 시스템(DMS) — 5가지 모델 아키텍처 정의서
공통 프레임워크
모든 모델은 입력으로 차량 내 운전자 영상(비디오 프레임 시퀀스)을 받는다. 눈 영역의 차폐 여부(선글라스, 마스크, 모자 등)는 이진 분기로 처리하지 않고, Adaptive Fusion 방식을 사용한다. 차폐 confidence score를 산출한 뒤, 이 score를 가중치로 활용하여 눈 영역 feature와 머리/몸통 feature를 soft blending한다. 차폐도가 높으면 머리/몸통 feature에 가중치가 올라가고, 차폐도가 낮으면 눈 영역 feature를 더 활용한다. 이를 통해 부분 차폐 상황에서도 정보 손실 없이 유연하게 대응한다.
최종 출력은 운전자의 주시 태스크 분류이며, 클래스는 사이드미러 주시, 백미러 주시, 전방 주시, 네비게이션 주시, 핸드폰 주시, 뒤돌아봄 등이다.

Case 1 — 랜드마크 + 스켈레톤 → 직접 태스크 분류
입력 이미지에서 머리 랜드마크(얼굴 키포인트)와 몸통 스켈레톤(어깨, 목, 머리 등 상체 키포인트)을 추출한다. 비차폐 시에는 눈 영역 feature도 함께 추출하여 Adaptive Fusion으로 결합한다. 이 결합된 feature를 시계열로 누적한 뒤, Spatial-Temporal Graph Convolution Network로 키포인트 간 공간적 관계를 인코딩하고, Transformer로 시간축 의존성을 모델링한다. 최종적으로 Classification Head를 통해 태스크를 분류한다. 이 모델은 시선 추정이라는 중간 단계 없이 랜드마크와 스켈레톤 정보로부터 직접 행동을 분류하는 것이 핵심이다.
Case 2 — 랜드마크 기반 시선 추정 → 태스크 분류
입력 이미지에서 머리 랜드마크를 추출한 뒤, 이 랜드마크 좌표를 입력으로 시선 방향(yaw, pitch)을 추정하는 모듈을 거친다. 즉, 랜드마크라는 중간 표현을 명시적으로 거쳐서 시선을 추정하는 2-stage 구조다. 추정된 시선 벡터와 몸통 스켈레톤 시퀀스를 결합하고, Adaptive Fusion을 적용한 뒤 Sequence Model(LSTM 또는 Transformer)에 넣어 시간축 패턴을 학습한다. 최종 Classification Head에서 태스크를 분류한다.
Case 3 — 이미지 직접 시선 추정 → 태스크 분류
입력 이미지에서 랜드마크 추출 과정 없이 CNN(ResNet 등)으로 이미지 자체에서 직접 시선 방향(yaw, pitch)을 추정한다. Case 2와 달리 랜드마크라는 중간 단계가 없는 end-to-end 시선 추정이다. 이후 흐름은 Case 2와 동일하다. 추정된 시선 벡터와 몸통 스켈레톤 시퀀스를 결합하고, Adaptive Fusion 적용 후 Sequence Model을 거쳐 태스크를 분류한다.
Case 4 — 머리 포즈 + 스켈레톤 베이스라인
입력 이미지에서 머리 포즈(yaw, pitch, roll 3축 회전)와 몸통 스켈레톤을 추출한다. Adaptive Fusion이나 눈 영역 feature 없이, 순수하게 머리 포즈 값과 몸통 스켈레톤만으로 시퀀스를 구성하고, 간단한 분류기(MLP 또는 경량 Temporal Model)로 태스크를 분류한다. 머리 포즈가 태스크 분류에 얼마나 기여하는지를 확인하기 위한 비교 실험용 베이스라인이다.
Case 5 — 스켈레톤 only 최소 베이스라인
입력 이미지에서 몸통 스켈레톤(어깨, 목, 머리 등 상체 키포인트)만 추출한다. 머리 포즈도, 시선 추정도, 눈 영역 정보도 전혀 사용하지 않는다. 스켈레톤 시퀀스만으로 간단한 분류기에 넣어 태스크를 분류한다. 몸통 움직임만으로 운전자의 주시 방향을 얼마나 구분할 수 있는지 하한선을 측정하기 위한 최소 베이스라인이다.

Case 간 핵심 차이 요약
Case 1은 시선 추정 없이 랜드마크와 스켈레톤으로 직접 분류한다. Case 2와 3은 시선 추정을 중간 단계로 거치되, Case 2는 랜드마크를 경유하고 Case 3은 이미지에서 직접 추정한다. Case 4와 5는 베이스라인으로, Case 4는 머리 포즈+스켈레톤, Case 5는 스켈레톤만 사용한다. Adaptive Fusion은 Case 1, 2, 3에만 적용되고 Case 4, 5에는 적용되지 않는다.
